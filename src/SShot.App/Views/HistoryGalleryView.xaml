<UserControl x:Class="SShot.App.Views.HistoryGalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="700">
    <Border Style="{StaticResource SurfaceCardStyle}" Padding="6" MinHeight="110">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource HistoryItemButtonStyle}" Margin="4" Click="OnItemClick" Tag="{Binding}" ToolTip="{Binding CapturedAt}">
                            <StackPanel>
                                <Image Source="{Binding Thumbnail}" Width="160" Height="90" Stretch="Uniform" />
                                <TextBlock Text="{Binding CapturedAt, StringFormat='yyyy/MM/dd HH:mm:ss'}"
                                           Foreground="{DynamicResource ThemeTextSecondary}"
                                           FontSize="10" HorizontalAlignment="Center" Margin="0,2,0,0" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Border>
</UserControl>
